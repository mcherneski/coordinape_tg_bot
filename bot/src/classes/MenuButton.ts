import { Telegraf } from "telegraf"

class UserMenu {
    private chatId: number
    private cirleId: number

    constructor(chatId: number, circleId: number) {
        this.chatId = chatId
        this.cirleId = circleId
    }

}